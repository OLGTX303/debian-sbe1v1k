# wififw mounts a SquashFS from the eMMC WIFIFW partition. On one boot it never
# returned, which stalled sbe1v1k-drivers (Requires=wififw) indefinitely. A
# oneshot that can hang has no business blocking boot without a bound.
[Service]
TimeoutStartSec=45s
